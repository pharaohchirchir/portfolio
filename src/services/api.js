export const sendContactForm = async (formData) => {
  // Placeholder: integrate email service like EmailJS, SendGrid or API endpoint.
  return new Promise((resolve) => {
    setTimeout(() => resolve({ success: true }), 1000);
  });
};
